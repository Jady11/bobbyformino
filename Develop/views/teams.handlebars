{{#each gifs as |gif|}}
      <div class="col-6 pt-3">
        <div class="row align-self-center justify-content-center text-center">
          <h3>{{gif.team}}</h3>
          </div>

        <div class="row align-self-center justify-content-center no-gutters" id="gif-div">
          <img src="{{{gif.url}}}">
        </div>
      </div>
{{/each}}

